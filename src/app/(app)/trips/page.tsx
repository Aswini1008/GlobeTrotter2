import { EmptyState } from '@/components/dashboard/empty-state';

export default function TripsPage() {
  // For now, this just shows the same empty state as the dashboard.
  // This can be built out later to show a list of all trips.
  return <EmptyState />;
}
